<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.project2.bodycheck" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >

        <activity android:name=".main.UserHabitActivity" />
        <activity android:name=".main.ContentActivity" />
        <activity android:name=".main.UserInfoActivity" />
        <activity android:name=".login.FindActivity" />
        <activity android:name=".main.SurveyActivity" />
        <activity android:name=".login.RegisterActivity" />
        <activity android:name=".calendar.CalendarActivity" />
        <activity android:name=".main.ToDoListActivity"/>
        <activity android:name=".main.ToDoListCreate"/>
        <activity android:name=".main.ToDoListChecking"/>
        <activity android:name=".login.LoginActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>